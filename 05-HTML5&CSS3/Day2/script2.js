widthDim = document.getElementsByTagName("body")[0];
length = Math.sqrt(2*widthDim*widthDim)/4;

lines = document.getElementsByTagName("line");
for (let x = 0, y = 0 ,idx = 0; x < length; idx++) {
    setTimeout(function(){
        
    },1000*idx);
}